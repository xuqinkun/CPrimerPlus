#include <stdio.h>
#include <string.h>

int main()
{
    long int estine;
    long johns;

    short int erns;
    short jobs;

    unsigned short a = -1;  // 65535

    printf("a = %u\n", a);

    printf("sizeof(int) = %lu, sizeof(short) = %lu, sizeof(long) = %lu\n", sizeof(int), sizeof(jobs), sizeof(estine));
    return 0;
}